// ignore_for_file: constant_identifier_names

// responsive small size width and height for the app
const double MIN_WIDTH = 350, MIN_HEIGHT = 200,
    SMALL_SCREEN_SIZE = 600,
    MEDIUM_SCREEN_SIZE = 900;
